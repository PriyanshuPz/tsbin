{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "vercel:deps": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "^build:wasm"],
      "outputs": ["dist/**"]
    },
    "dev": { "persistent": true, "cache": false },
    "lint": {},
    "test": {}
  }
}
